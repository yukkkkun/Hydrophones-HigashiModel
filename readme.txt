Anacondaをインストール（最新版で良いと思う）
↓
Anaconda Promptを開く
↓
jupyter notebook
とタイプし，エンター
↓
Webブラウザ上でJupyter notebookが開くので，ファイルを探して，開く
ファイルは，notebookの中にあります．
↓
ファイルをRun all，セルごとに実行など


github上でデータ管理していますが，各自でアップデートしていってもらっても構いません．
一応，研究データを公表しないため，
データとグラフはアップされないようになっています
（gitignoreの設定）